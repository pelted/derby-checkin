en:
  date:
    formats:
      full_date: '%A, %B %e, %Y'
  time:
    formats:
      simple_time: '%l:%M %P'

  activerecord:
    errors:
      models:
        race:
          attributes:
            location:
              blank: 'should be provided so racers know where to go'
            date:
              on_or_after: 'can not be in the past'
            time:
              on_or_after: 'should be something reasonable'